# Data Quality and Exploratory Data Analysis Report

Исходный файл: `example.csv`

Строк: **36**, столбцов: **14**

## Компактная сводка

Полные данные о размерах, Quality Score и проблемных колонках доступны в файле `summary.json`.

## Качество данных (эвристики)

- Интегральный показатель (Quality Score): **0.744**
- Порог для проблемных пропусков (--min-missing-share): **1.00%**
- Макс. доля пропусков по колонке: **5.56%**
- Слишком мало строк: **True**
- Слишком много колонок: **False**
- Слишком много пропусков: **False**
- Константные колонки: **False** []
- Есть колонки со слишком большим количеством нулей (>80%): **False**

- Минимальное число уникальных значений в колонке: **2**
- Максимальная доля нулей в числовых колонках: **66.67%**

## Колонки

См. файл `summary.csv`.

## Проблемные колонки (Missing Data)

Следующие колонки имеют долю пропусков ≥ **1.00%**:

```text
                          missing_count  missing_share
city                                  2       0.055556
avg_session_duration_min              2       0.055556
```



## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

См. файлы в папке `top_categories/`.

## Гистограммы числовых колонок

См. файлы `hist_*.png`.
